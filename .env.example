# 环境变量示例(本地开发可复制为 .env.local)
# 构建时由 GitHub Actions 注入,本地开发可留空使用 hostname 推断

# 部署环境:dev / staging / prod
VITE_ENV=dev

# PR 编号(仅 dev 环境)
VITE_PR_NUMBER=

# 站点公开 URL(用于 OG / canonical)
VITE_PUBLIC_URL=http://localhost:5173

# 构建对应的 commit SHA
VITE_COMMIT_SHA=
